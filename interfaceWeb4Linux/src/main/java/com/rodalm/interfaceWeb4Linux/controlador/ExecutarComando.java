package com.rodalm.interfaceWeb4Linux.controlador;

import com.jcraft.jsch.ChannelExec;

import java.io.InputStream;

public class ExecutarComando {
/*
    public ExecutarComando(Conexao conexao) {

    ChannelExec canal = (ChannelExec) conexao.getSessao().openChannel("exec");
    canal.setCommand("tail -f /var/log/squid/acces.log | gre rodrigo.almeida");
    canal.setInputStream(null);
    canal.setErrStream(System.err);

    InputStream in = canal.getInputStream();
    canal.connect();

    byte[] buffer = new byte[8192];
    StringBuilder retornoSaida = new StringBuilder();

    try {
        while (!shouldStop){

        }
    }
    }
*/
}
